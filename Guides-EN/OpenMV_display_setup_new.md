### NOTE: this guide only applies to firmware version 4.8.1 or later, for firmware version 4.4.1 or earlier use this [Guide](Display_legacy/OpenMV_display_setup_legacy.md)

### 1. Required steps
First it needs the board is flashed with **OpenMV**  firmware

(the steps to do so are here:  
[OpenMV Flashing (Windows)](Windows/OpenMV_firmware_flash.md) | [OpenMV Flashing (Linux)](Linux/OpenMV_firmware_flash.md))

---


### 2. Sample code in the repository

in the repository folder **Display/4.8.1** you will find a file called  **main.py** where you can use the display.

currently `main.py` includes a sample to view the camera feed.
